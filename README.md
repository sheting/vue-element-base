# Vue Element UI Template demo

#### start

```powershell
>cd /motcert-app/
> yarn run serve # open browser via 'localhost:8888' or '127.0.0.1:8888'
```

#### build

```powershell
>cd /motcert-app/
> yarn run build
```